/**
 * Created by jzl on 16/4/28.
 */
import groovy.swing.SwingBuilder
import javax.swing.*

class parsing {
    def well_defined = "well defined"
    def not_well_defined = "not well defined"

    void parse_input(input) {
        if (input[0] != '(') {
            println(not_well_defined)
            return
        }

        
    }
}
